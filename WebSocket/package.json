{
  "name": "websocket",
  "version": "1.0.0",
  "description": "websocket demo for share",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "ws": "^6.2.1"
  }
}
